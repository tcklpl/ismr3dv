{
  "name": "ismr3d",
  "version": "1.0.0",
  "description": "ismr 3d visualizer",
  "main": "index.js",
  "scripts": {
    "minify-html": "npx html-minifier --collapse-whitespace --remove-comments --input-dir ./src/ --output-dir ./out/ --file-ext html",
    "compile-scss": "scss --no-cache --update src/style:out/style/",
    "sync-others": "rsync -a --exclude '*.html' --exclude '*.ts' --exclude '*.scss' src/ out/",
    "compile": "npm run minify-html && tsc && npm run sync-others && npm run compile-scss",
    "clean": "rm -r ./out/*"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jquery": "^3.5.13",
    "html-minifier": "^4.0.0"
  },
  "dependencies": {
    "jquery": "^3.6.0"
  }
}
